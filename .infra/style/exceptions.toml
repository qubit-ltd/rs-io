format = 1

[[exceptions]]
rule = "coverage-cfg"
path = "src/lib.rs"
reason = "Re-export coverage-only allocation and copy helpers for cfg(coverage) integration tests."

[[exceptions]]
rule = "coverage-cfg"
path = "src/util/allocation.rs"
reason = "Keep deterministic allocation failure hooks local to rs-io because cfg(coverage) does not propagate into registry dependencies."

[[exceptions]]
rule = "coverage-cfg"
path = "src/util/mod.rs"
reason = "Re-export coverage-only helpers from private utility modules for cfg(coverage) integration tests."

[[exceptions]]
rule = "coverage-cfg"
path = "src/util/streams.rs"
reason = "Expose a coverage-only entry point for the copied-item count overflow branch that cannot be reached with practical test input."
